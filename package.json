{
  "name": "plannke",
  "private": true,
  "scripts": {
    "test": "node --check product.js && node --check product-core.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}

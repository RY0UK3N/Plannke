name: Promote presentation metadata once

on:
  push:
    branches:
      - refactor/promote-presentation-metadata

permissions:
  contents: write

jobs:
  promote-presentation-metadata:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/promote-presentation-metadata
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Promote presentation metadata
        run: node scripts/promote-presentation-metadata-once.js
      - name: Validate repository
        run: npm test
      - name: Commit canonical presentation metadata
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: promote presentation metadata"
          git push origin HEAD:refactor/promote-presentation-metadata

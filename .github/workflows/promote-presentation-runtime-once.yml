name: Promote presentation runtime once

on:
  push:
    branches:
      - refactor/promote-presentation-runtime

permissions:
  contents: write

jobs:
  promote-presentation-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/promote-presentation-runtime
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Promote presentation runtime
        run: node scripts/promote-presentation-runtime-once.js
      - name: Validate repository
        run: npm test
      - name: Commit canonical presentation runtime
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: promote presentation runtime name"
          git push origin HEAD:refactor/promote-presentation-runtime

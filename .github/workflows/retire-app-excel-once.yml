name: Retire app Excel once

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

permissions:
  contents: write

jobs:
  retire-excel:
    if: github.head_ref == 'refactor/retire-app-excel-copies'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/retire-app-excel-copies
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Retire legacy app Excel runtime
        run: node scripts/retire-app-excel-once.js
      - name: Validate repository
        run: npm test
      - name: Commit cleaned runtime
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Retire legacy app Excel runtime"
          git push origin HEAD:refactor/retire-app-excel-copies
